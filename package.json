{
  "name": "minista-project",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "minista",
    "build": "minista build",
    "preview": "minista preview",
	"deploy": "git push origin --delete gh-pages && npm run build && git add dist -f && git commit -m 'deploy' && git subtree push --prefix dist origin gh-pages && git reset --hard @~"
  },
  "prettier": {
    "semi": false
  },
  "devDependencies": {
    "minista": "^3.1.12",
    "postcss-pxtorem": "^6.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "sass": "^1.93.2"
  },
  "dependencies": {
    "@a1rth/css-normalize": "^1.2.4",
    "clsx": "^2.1.1",
    "imask": "^7.6.1",
    "swiper": "^12.0.3"
  }
}
